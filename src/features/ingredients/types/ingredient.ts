/**
 * Ingredients are owned by the Inventory feature.
 * Keep this folder only as a compatibility alias for agents and imports.
 */

export type {
  CreateIngredientInput,
  Ingredient,
  IngredientCategory,
  IngredientFormValues,
  IngredientWithRelations,
  UpdateIngredientInput,
} from "@/features/inventory/types/inventory";
